rules:
  - id: homelab-hardcoded-secret
    category: general
    title: Hardcoded HomeLab Secret
    severity: HIGH
    keywords:
      - homelab_secret
    regex: 'homelab_secret\s*[:=]\s*["'']?(?P<secret>[A-Za-z0-9_-]{8,})'
    secret-group-name: secret
